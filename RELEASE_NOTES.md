# Sambi_DB v1.0.0

🎉 We are excited to announce the first official release of **Sambi_DB**!

Sambi_DB is a custom database engine built with .NET 8, designed for performance and ease of use. It comes with a built-in modern Admin Dashboard for managing your data effortlessly.

## 🌟 Key Features

-   **Custom Database Engine**: High-performance backend built on .NET 8.
-   **Admin Dashboard**: Fully responsive React-based interface.
-   **Windows Service Integration**: Installs and runs automatically as a background service.
-   **Swagger API**: Explore and test your database endpoints directly.
-   **Collection & Document Management**: Create and manage data with intuitive UI.

## 📦 Installation

This release includes an automated Windows installer (`SambiDbInstaller.exe`).

1.  **Download** the `SambiDbInstaller.exe` from the Assets section below.
2.  **Run** the installer. (Requires Administrator privileges).
3.  **Finish**: The installer will:
    -   Copy the necessary files.
    -   Register **SambiDb** as a Windows Service.
    -   Automatically start the service.

## 🚀 Post-Installation

Once installed, the service will be running in the background.
You can access the Admin Dashboard at:
`http://localhost:<PORT>` (Default port is usually 5000 or 5001)

## 🛠 Assets

-   `SambiDbInstaller.exe`: The complete installer for Windows (x64).
